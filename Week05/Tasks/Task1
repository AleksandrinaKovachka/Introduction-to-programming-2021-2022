#include <iostream>

using namespace std;

int main()
{
  int k, n;
	bool haveNumber = false;

	cin >> k;
	cin >> n;

	for (int i = 0; i <= n; ++i)
	{
		if (i == k)
		{
			haveNumber = true;
			break;
		}
	}

	haveNumber ? cout << "Yes\n" : cout << "No";
  return 0;
}
